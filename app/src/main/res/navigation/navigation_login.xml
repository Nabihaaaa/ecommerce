<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation_login"
    app:startDestination="@id/loginFragment">

    <fragment
        android:id="@+id/loginFragment"
        android:name="com.example.tokon.fragment.LoginFragment"
        android:label="fragment_login"
        tools:layout="@layout/fragment_login" >
        <action
            android:id="@+id/action_loginFragment2_to_registrasiFragment"
            app:destination="@id/registrasiFragment" />
    </fragment>
    <fragment
        android:id="@+id/registrasiFragment"
        android:name="com.example.tokon.fragment.RegistersFragment"
        android:label="fragment_registrasi"
        tools:layout="@layout/fragment_registers" />
</navigation>